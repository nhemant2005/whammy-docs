# Issue 16.2 — Upload page dark mode + Tea Green radial glow

**GitHub:** #18
**Label:** done
**Blocked by:** Issue #17 (16.1)

## What was built

`upload.html` fully rewritten with the Lumina Docs dark theme. All colours via CSS custom properties — no raw hex values in template HTML.

**Component styles (added in `<style>` block):**
- `.upload-card` — `var(--color-surface)` background, `var(--color-border)` border, 8px radius
- `.drop-zone` — `var(--color-base)` background, dashed `var(--color-border)` border; hover/drag-over state adds `var(--color-primary)` border and a faint Periwinkle tint
- `.mode-label` — `var(--color-base)` background, `var(--color-border)` border; `:has(input:checked)` state uses `var(--color-primary)` border + light Periwinkle tint
- `.btn-primary` — `var(--color-primary)` background, `var(--color-base)` text, 8px radius, full width
- `.error-banner` — `rgba(220,38,38,0.15)` background, rose border, `#fca5a5` text — visible against dark background
- `.gradient-seed` — `position: fixed; bottom: 0; height: 40vh; pointer-events: none; z-index: -1; background: radial-gradient(ellipse at bottom, rgba(200,255,190,0.25), transparent 70%)`

**Typography:**
- `<h1>` — `font-weight: 800` (Unageo ExtraBold via inherited font-family)
- Subtitle — `var(--color-text-muted)`, weight 400
- Mode selector legend — `var(--color-text-muted)`, weight 500
- Mode option labels — `var(--color-text)`, weight 500; descriptions `var(--color-text-muted)`

**Other details:**
- Drag-over JS handler adds/removes `.drag-over` class (replaced Tailwind class toggles)
- File name indicator uses `var(--color-accent)` (Tea Green) when a file is selected
- "Recommended" badge uses Periwinkle tint on dark background instead of indigo-on-white
- Sample project button: inline style overrides for dark-compatible hover underline

## Acceptance criteria

- [x] Upload page background is `var(--color-base)` (dark)
- [x] All interactive elements use the dark colour palette
- [x] Primary action button uses `var(--color-primary)` background with dark text
- [x] Tea Green radial glow div is fixed at the bottom of the viewport, pointer-events none
- [x] All text uses Unageo via inherited font-family
- [x] Headings use font-weight 800, buttons/labels 500, body/placeholders 400
- [x] No raw hex values in template HTML
- [x] All 76 pytest tests still pass
