# Issue 16.3 — Preview page dark shell (cards, TOC, buttons, panels)

**GitHub:** #19
**Label:** ready-for-agent
**Blocked by:** Issue #17 (16.1)

## What to build

Apply the dark theme to all UI chrome in `preview.html`. No change to functionality — only visual treatment.

**Section cards:**
- Background: `var(--color-surface)`, border: 1px solid `var(--color-border)`, `border-radius: 8px`
- Card header: Unageo Medium (500) in `var(--color-text)`
- Active card during generation: Periwinkle left border accent instead of indigo
- Completed card badge: Tea Green accent instead of green
- Rendered markdown content (`.prose`): override prose colours for dark background — headings near-white, body `var(--color-text)`, code blocks dark surface

**TOC sidebar:**
- Background: `var(--color-surface)`, border `var(--color-border)`
- TOC links: `var(--color-text-muted)`, active link: `var(--color-primary)`

**Buttons (all instances — generating state hidden buttons too):**
- Download ZIP: `background: var(--color-primary)`, `color: var(--color-base)`, `border-radius: 8px`
- Edit: dark ghost style — `var(--color-surface)` background, `var(--color-border)` border, `var(--color-text)` text
- Regenerate with AI (idle): same ghost style with Periwinkle text
- Regenerate with AI (cancel state): muted ghost with `var(--color-text-muted)` text
- Generate button inside feedback panel: `var(--color-primary)` background

**Feedback panel (inline regen textarea):**
- Panel background: slightly darker than surface, `var(--color-border)` border
- Textarea: `var(--color-base)` background, `var(--color-text)` text, `var(--color-border)` border, `border-radius: 8px`

**Unsaved-changes indicator:** `var(--color-accent)` (Tea Green) dot or text, visible against dark surface

**Error banner:** red/rose background toned for dark mode, near-white text — must not be missed against the dark page

**Generating state skeletons:** pulse animation on dark surface (`var(--color-surface)`) rather than light grey

No raw hex values in template HTML.

## Acceptance criteria

- [ ] Section cards use `var(--color-surface)` + `var(--color-border)` border
- [ ] Rendered markdown prose is legible against dark card surface
- [ ] TOC sidebar matches dark palette
- [ ] Download ZIP uses `var(--color-primary)` background
- [ ] Edit / Regenerate with AI / Generate buttons styled correctly in dark theme
- [ ] Feedback panel textarea is dark-themed
- [ ] Unsaved-changes indicator visible in dark mode
- [ ] Error banner clearly distinguishable in dark mode
- [ ] Skeleton pulse animation works on dark surface
- [ ] No raw hex values in template HTML
- [ ] All 74+ pytest tests still pass
